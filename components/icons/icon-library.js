// Icon library — placeholder (ships empty/agnostic).
// Generate the real one from a folder of SVGs:
//   node tools/icons/build-icon-library.mjs <svg-dir> --accent #<brandAccent> --out components/icons/icon-library.js
// It overwrites this file with IconKit.installIcon(...) calls. Loaded after
// icon-kit.js everywhere the kit loads, so installed icons appear in the Icon
// Library + Visual Designer (recolourable) and open editable in the icon editor.
