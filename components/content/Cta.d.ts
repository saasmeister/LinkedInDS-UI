import React from "react";

export interface CtaProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
/** Closing call-to-action bar for a carousel back cover. */
export function Cta(props: CtaProps): JSX.Element;
